<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/sveglia_nav_graph"
    app:startDestination="@id/svegliaFragment">


    <fragment
        android:id="@+id/svegliaFragment"
        android:name="it.unimib.brain_alarm.SvegliaFragment"
        android:label="fragment_sveglia"
        tools:layout="@layout/fragment_sveglia" >
        <action
            android:id="@+id/action_svegliaFragment_to_mainActivity"
            app:destination="@id/mainActivity" />
    </fragment>
    <include app:graph="@navigation/menu_nav_graph" />
    <activity
        android:id="@+id/mainActivity"
        android:name="it.unimib.brain_alarm.ui.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
</navigation>